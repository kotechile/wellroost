---
const navLinks = [
	{ href: '/', label: 'Home' },
	{ href: '/calculators/', label: 'Calculators' },
	{ href: '/categories/', label: 'Categories' },
	{ href: '/about/', label: 'About' }
];

const pathname = Astro.url.pathname;
---

<header class="fixed inset-x-0 top-0 z-50 flex justify-center bg-white/85 backdrop-blur-md border-b border-gray-200">
	<div class="w-full max-w-7xl px-4 sm:px-6 lg:px-8">
		<div class="flex h-16 items-center justify-between">
			<!-- Logo -->
			<a
				href="/"
				class="inline-flex items-center gap-2.5 font-bold tracking-tight text-gray-1000 transition hover:opacity-80 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary"
			>
				<span class="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-gray-1000 text-white text-sm font-black">
					W
				</span>
				<span class="text-lg">WellRoost</span>
			</a>

			<!-- Desktop Nav -->
			<nav class="hidden items-center gap-6 md:flex" aria-label="Primary navigation">
				{
					navLinks.map((link) => {
						const isActive =
							pathname === link.href ||
							(link.href !== '/' && pathname.startsWith(`${link.href}/`));
						return (
							<a
								href={link.href}
								class:list={[
									'text-sm font-medium transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary',
									isActive
										? 'text-gray-1000 font-semibold'
										: 'text-gray-600 hover:text-gray-1000'
								]}
								aria-current={isActive ? 'page' : undefined}
							>
								{link.label}
							</a>
						);
					})
				}
			</nav>

			<!-- Mobile Actions / Search -->
			<div class="flex items-center gap-2 md:gap-4">
				<button
					type="button"
					data-search-open
					aria-label="Open site search"
					class="inline-flex h-9 w-9 items-center justify-center rounded-lg text-gray-600 transition hover:bg-gray-100 hover:text-black focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary border border-transparent hover:border-gray-200"
				>
					<svg viewBox="0 0 24 24" fill="none" class="h-5 w-5" aria-hidden="true">
						<path
							d="M21 21l-4.35-4.35m1.35-5.15a6.5 6.5 0 1 1-13 0 6.5 6.5 0 0 1 13 0Z"
							stroke="currentColor"
							stroke-width="1.8"
							stroke-linecap="round"
							stroke-linejoin="round"
						/>
					</svg>
				</button>

				<details class="group relative md:hidden">
					<summary
						class="inline-flex h-9 w-9 cursor-pointer list-none items-center justify-center rounded-lg text-gray-600 transition hover:bg-gray-100 hover:text-black focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary border border-transparent hover:border-gray-200"
						aria-label="Open navigation menu"
					>
						<svg viewBox="0 0 24 24" fill="none" class="h-5 w-5" aria-hidden="true">
							<path
								d="M4 7h16M4 12h16M4 17h16"
								stroke="currentColor"
								stroke-width="1.8"
								stroke-linecap="round"
							/>
						</svg>
					</summary>
					<div class="absolute right-0 top-[calc(100%+0.5rem)] w-56 rounded-xl border border-gray-200 bg-white p-2 shadow-xl">
						<nav class="grid gap-1" aria-label="Mobile navigation">
							{
								navLinks.map((link) => {
									const isActive =
										pathname === link.href ||
										(link.href !== '/' && pathname.startsWith(`${link.href}/`));
									return (
										<a
											href={link.href}
											class:list={[
												'rounded-lg px-3 py-2 text-sm transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary',
												isActive ? 'bg-gray-100 text-black font-semibold' : 'text-gray-600 hover:bg-gray-50 hover:text-black'
											]}
											aria-current={isActive ? 'page' : undefined}
										>
											{link.label}
										</a>
									);
								})
							}
						</nav>
					</div>
				</details>
			</div>
		</div>
	</div>
</header>
